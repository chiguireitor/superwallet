WALLETS = {
    "DEFINEYOURSYMBOLS": { # don't leave this symbol enabled, or the super wallet will complain
        "user": "YOUR_USER", # User as in .con file rpcuser
        "pass": "YOUR_PASSWORD", # Password as in .conf file rpcpassword
        "host": "HOST_ADDRESS", # Host of the wallet
        "port": 12345 # Port of the wallet's RPC port
    }   
}